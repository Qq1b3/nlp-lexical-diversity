# Python (keep caches tracked per user's request)
# __pycache__/
*.py[cod]
*$py.class
*.so
.Python
# Keep venv/ directory structure but ignore contents (can be changed if needed)
venv/
env/
*.egg-info/
dist/
build/

# Data directories (track datasets per user's request; keep processed_data ignored)
#processed_data/
# datasets/
# Still ignore any large archives anywhere
#*.tar.gz

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

